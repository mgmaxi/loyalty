/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'tiendamacro-v2-stg.avenida.com'],
    unoptimized: true,
  },
};

module.exports = nextConfig;
